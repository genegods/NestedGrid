import React from 'react'
import { DivisionData } from '../data/DivisionData'

const MainPage = () => {
  return (
    <React.Fragment>
    <section className='uppercase grid grid-cols-3 gap-1 mx-1 md:grid-cols-4 md:w-2/5 md:mx-auto'>
      <div className="grid grid-cols-4 h-40 w-40 font-bold text-white ">
          {
              DivisionData.map((item,index) =>{
                  return(
                      <div key={index} className={`centered ${item.color}`}>
                          <div className='w-8 h-8 centered'>
                              <span>{item.number}</span>
                          </div>
                      </div>
                  )
              })
          }
      </div>

      <div className="grid grid-cols-4 h-40 w-40 font-bold text-black">
          {
              DivisionData.map((item,index) =>{
                  return(
                      <div key={index} className={`centered ${item.color}`}>
                          <div className='w-8 h-8 centered'>
                              <span>{item.alphabet}</span>
                          </div>
                      </div>
                  )
              })
          }
      </div>

      <div className="grid grid-cols-4 h-40 w-40 font-bold text-white ">
          {
              DivisionData.map((item,index) =>{
                  return(
                      <div key={index} className={`centered ${item.color}`}>
                          <div className='w-8 h-8 centered'>
                              <span>{item.number}</span>
                          </div>
                      </div>
                  )
              })
          }
      </div>

      <div className="grid grid-cols-4 h-40 w-40 font-bold text-black">
          {
              DivisionData.map((item,index) =>{
                  return(
                      <div key={index} className={`centered ${item.color}`}>
                          <div className='w-8 h-8 centered'>
                              <span>{item.alphabet}</span>
                          </div>
                      </div>
                  )
              })
          }
      </div>


      <div className="grid grid-cols-4 h-40 w-40 font-bold text-white ">
          {
              DivisionData.map((item,index) =>{
                  return(
                      <div key={index} className={`centered ${item.color}`}>
                          <div className='w-8 h-8 centered'>
                              <span>{item.number}</span>
                          </div>
                      </div>
                  )
              })
          }
      </div>

      <div className="grid grid-cols-4 h-40 w-40 font-bold text-black">
          {
              DivisionData.map((item,index) =>{
                  return(
                      <div key={index} className={`centered ${item.color}`}>
                          <div className='w-8 h-8 centered'>
                              <span>{item.alphabet}</span>
                          </div>
                      </div>
                  )
              })
          }
      </div>


      <div className="grid grid-cols-4 h-40 w-40 font-bold text-white ">
          {
              DivisionData.map((item,index) =>{
                  return(
                      <div key={index} className={`centered ${item.color}`}>
                          <div className='w-8 h-8 centered'>
                              <span>{item.number}</span>
                          </div>
                      </div>
                  )
              })
          }
      </div>

      <div className="grid grid-cols-4 h-40 w-40 font-bold text-black">
          {
              DivisionData.map((item,index) =>{
                  return(
                      <div key={index} className={`centered ${item.color}`}>
                          <div className='w-8 h-8 centered'>
                              <span>{item.alphabet}</span>
                          </div>
                      </div>
                  )
              })
          }
      </div>

      

      <div className="grid grid-cols-4 h-40 w-40 font-bold text-white ">
          {
              DivisionData.map((item,index) =>{
                  return(
                      <div key={index} className={`centered ${item.color}`}>
                          <div className='w-8 h-8 centered'>
                              <span>{item.number}</span>
                          </div>
                      </div>
                  )
              })
          }
      </div>


      
      <div className="grid grid-cols-4 h-40 w-40 font-bold text-black">
          {
              DivisionData.map((item,index) =>{
                  return(
                      <div key={index} className={`centered ${item.color}`}>
                          <div className='w-8 h-8 centered'>
                              <span>{item.alphabet}</span>
                          </div>
                      </div>
                  )
              })
          }
      </div>


      <div className="grid grid-cols-4 h-40 w-40 font-bold text-white ">
          {
              DivisionData.map((item,index) =>{
                  return(
                      <div key={index} className={`centered ${item.color}`}>
                          <div className='w-8 h-8 centered'>
                              <span>{item.number}</span>
                          </div>
                      </div>
                  )
              })
          }
      </div>


      <div className="grid grid-cols-4 h-40 w-40 font-bold text-black">
          {
              DivisionData.map((item,index) =>{
                  return(
                      <div key={index} className={`centered ${item.color}`}>
                          <div className='w-8 h-8 centered'>
                              <span>{item.alphabet}</span>
                          </div>
                      </div>
                  )
              })
          }
      </div>

      <div className="grid grid-cols-4 h-40 w-40 font-bold text-white ">
          {
              DivisionData.map((item,index) =>{
                  return(
                      <div key={index} className={`centered ${item.color}`}>
                          <div className='w-8 h-8 centered'>
                              <span>{item.number}</span>
                          </div>
                      </div>
                  )
              })
          }
      </div>


      <div className="grid grid-cols-4 h-40 w-40 font-bold text-black">
          {
              DivisionData.map((item,index) =>{
                  return(
                      <div key={index} className={`centered ${item.color}`}>
                          <div className='w-8 h-8 centered'>
                              <span>{item.alphabet}</span>
                          </div>
                      </div>
                  )
              })
          }
      </div>


      <div className="grid grid-cols-4 h-40 w-40 font-bold text-white ">
          {
              DivisionData.map((item,index) =>{
                  return(
                      <div key={index} className={`centered ${item.color}`}>
                          <div className='w-8 h-8 centered'>
                              <span>{item.number}</span>
                          </div>
                      </div>
                  )
              })
          }
      </div>

      <div className="grid grid-cols-4 h-40 w-40 font-bold text-black">
          {
              DivisionData.map((item,index) =>{
                  return(
                      <div key={index} className={`centered ${item.color}`}>
                          <div className='w-8 h-8 centered'>
                              <span>{item.alphabet}</span>
                          </div>
                      </div>
                  )
              })
          }
      </div>

      
      <div className="grid grid-cols-4 h-40 w-40 font-bold text-white ">
          {
              DivisionData.map((item,index) =>{
                  return(
                      <div key={index} className={`centered ${item.color}`}>
                          <div className='w-8 h-8 centered'>
                              <span>{item.number}</span>
                          </div>
                      </div>
                  )
              })
          }
      </div>


      <div className="grid grid-cols-4 h-40 w-40 font-bold text-black">
          {
              DivisionData.map((item,index) =>{
                  return(
                      <div key={index} className={`centered ${item.color}`}>
                          <div className='w-8 h-8 centered'>
                              <span>{item.alphabet}</span>
                          </div>
                      </div>
                  )
              })
          }
      </div>

      <div className="grid grid-cols-4 h-40 w-40 font-bold text-white ">
          {
              DivisionData.map((item,index) =>{
                  return(
                      <div key={index} className={`centered ${item.color}`}>
                          <div className='w-8 h-8 centered'>
                              <span>{item.number}</span>
                          </div>
                      </div>
                  )
              })
          }
      </div>


      <div className="grid grid-cols-4 h-40 w-40 font-bold text-black">
          {
              DivisionData.map((item,index) =>{
                  return(
                      <div key={index} className={`centered ${item.color}`}>
                          <div className='w-8 h-8 centered'>
                              <span>{item.alphabet}</span>
                          </div>
                      </div>
                  )
              })
          }
      </div>


    </section>
  </React.Fragment>
  )
}

export default MainPage